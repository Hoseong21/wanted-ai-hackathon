# LLM
OPENAI_API_KEY=
LLM_MODEL=gpt-5.6-luna
LLM_TEMPERATURE=0

# ANTHROPIC_API_KEY=

# rag
CHROMA_PERSIST_DIR=src/rag/vectorstore

# kure(기본값, 한국어 특화, 로컬 실행) | hash(네트워크/모델 다운로드 불가 환경용 대체) | openai(OPENAI_API_KEY 필요)
EMBEDDING_BACKEND=kure

# budget db
BUDGET_DB_PATH=data/budget.db